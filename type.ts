const date = new Date();

console.log(`date is ${date.getDate()}`);

const person = {
	age: 30,
};

console.log(`age is  ${person.age}`);

class Color {
	background = "white";
}
const red = new Color();

console.log(`red background is ${red.background}`);
